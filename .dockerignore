.git/
test-*.json
mma-source/
tools/
**/node_modules/
**/target/
**/.venv/
**/.*
!.nvmrc
# Plugin sidecars, models and eval data are never part of the app build; they are
# gigabytes that would otherwise ship as build context on every image build.
plugins/*/sidecar/
plugins/*/eval/
plugins/automapper/
