[package]
name = "mma-geocode"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
reverse_geocoder = "4"
fastrand = "2"
mma-geo = { path = "../geo" }
criterion = "0.5"

[[bench]]
name = "nearest"
harness = false
