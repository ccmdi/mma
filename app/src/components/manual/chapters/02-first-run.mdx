export const title = "Getting started";

<App /> stores everything in a single application data folder. You never have to manage files by hand, but it helps to know where they are.

## The data folder

- **Windows:** `%APPDATA%\app.map-making.local`
- **Linux:** `~/.local/share/app.map-making.local`
- **macOS:** `~/Library/Application Support/app.map-making.local`

Open it directly from **Settings → Advanced → Data → Open data folder**. It holds your maps, tags, version history, the seen history, logs, and any installed plugins.

<Note>

This is a work in progress and data loss can _rarely_ happen. Keep backups. The recommended backup is the **Export** (all maps as a ZIP) button on the home screen. See <ChapterLink id="bulk-backup" />.

</Note>

## Logs

A unified log file (`mma.log`) is written inside the data folder under `logs/`. It is the first thing to grab when reporting a bug. Open it directly from **Settings → Advanced → Debug → Open log file**.
