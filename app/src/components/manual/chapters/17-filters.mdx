export const title = "Filters";

A **Filter** selection queries a field on each location and selects the matches. Build one with **Filter by metadata...** (command palette, or its button in the pinned toolbar, where the form opens inline), or edit an existing one from its selection menu.

## Fields you can query

- **Built-in fields.** **Latitude** and **Longitude**, **ID**, **Created** and **Modified** timestamps, **Pano ID**, **Load as pano ID**, **Tag count**, **Heading**, **Pitch**, and **Zoom**.
- **Metadata fields.** Any field present on the map, such as **countryCode**, **cameraType**, **panoType**, **altitude**, **imageDate**, exact date, timezone, and any custom fields you have added. Custom fields can be typed as text, number, date, month, enum, or array.

## Operators

- **= / !=** equals and not equals.
- **&gt; / &lt; / &gt;= / &lt;=** numeric and date comparisons.
- **between** a range, with **between (any year)** and **between (any date)** variants for date and month fields, so you can match, for example, "any year, June to August".
- **has / does not have** presence of a value.
- **contains / does not contain** for array fields, plus length-based comparisons (length =, length >, length between, etc.).

Enum fields (such as camera type) offer only the equality operators and a value list. Date and month fields use a date picker, including any-year mode for season-style filters. A date pick at midnight means "the whole day" - the filter spans the full calendar day. Use the clear-time button on the picker to reset to day-grain.

## Timezone

Date filters compare against your local timezone by default. Check **Location timezone** on the filter to compare in each location's own timezone instead, which is important when filtering by time of day across many countries.

## Stepping through periods

Filter selection rows for date and number ranges show left/right arrow buttons. These shift the filter window forward or backward by its own span, so you can step through dates day-by-day or numbers band-by-band. Between the min and max inputs, copy buttons let you duplicate one bound to the other.

<Note>

Filters operate on stored metadata. Run enrichment first so the fields you want to filter on exist. See <ChapterLink id="enrichment" />.

</Note>
