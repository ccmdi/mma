export const title = "Importing into a map";

The **Import file** button in the bottom bar brings locations into the current map. You can also **drag a JSON or CSV file** straight onto the editor window; a "Drop file to import" overlay appears while you hover. Either way an import panel opens in the sidebar showing a preview before anything is added. Staged locations appear on the map in a distinct color (configurable in **Settings → Map → Markers → Staged marker color**).

## The import panel

- **Location count.** How many locations the file contains.
- **Tags in file.** The tags detected in the file.
- **Fields.** Every metadata field found, each with its value count and a checkbox. Untick a field to drop it on import; your choices are remembered.
- **Tag all imported locations.** Apply one tag to everything you import.
- **Warnings.** Any problems detected during parsing.

## Formats and field names

<App /> auto-detects **JSON** vs **CSV** and tolerates common field-name variants (for example latitude/lat, longitude/lng/lon, pano/panoId), so files from various tools usually import without manual mapping. Press **Import** to add the locations, or **Discard** to cancel.

<Note>

Very large imports (500,000+ locations) auto-commit immediately and cannot be undone. A confirmation dialog warns you before this happens.

</Note>
