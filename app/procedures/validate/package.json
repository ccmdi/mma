{
	"name": "mma-procedure-validate",
	"version": "0.1.0",
	"private": true,
	"description": "Street View coverage validation procedure (Run shape) for the MMA procedure engine",
	"type": "module",
	"scripts": {
		"build": "node ../../scripts/build-procedures.mjs validate",
		"test": "npm run build && node --test \"test/*.test.mjs\""
	}
}
