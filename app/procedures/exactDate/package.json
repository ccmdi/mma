{
	"name": "mma-procedure-exactdate",
	"version": "0.1.0",
	"private": true,
	"description": "Exact capture timestamp procedure (Run shape) for the MMA procedure engine",
	"type": "module",
	"scripts": {
		"build": "node ../../scripts/build-procedures.mjs exactDate",
		"test": "npm run build && node --test \"test/*.test.mjs\""
	}
}
