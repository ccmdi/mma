{
	"name": "mma-procedure-panoresolve",
	"version": "0.1.0",
	"private": true,
	"description": "Pano id from coordinates procedure (Run shape) for the MMA procedure engine",
	"type": "module",
	"scripts": {
		"build": "node ../../scripts/build-procedures.mjs panoResolve",
		"test": "npm run build && node --test \"test/*.test.mjs\""
	}
}
